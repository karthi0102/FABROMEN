<% if(success && success.length) {%>
    <script>
        let success = '<%= success%>';
        swal("FABROMEN", success, "success");
      </script>
  <% } %>
  <% if(error && error.length) {%>
    <script>
      let error= '<%= error%>';
      swal("FABROMEN", error , "danger");
    </script>
  <% } %>